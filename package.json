{
  "name": "class-unknown-ios",
  "private": true,
  "scripts": {
    "cap:sync": "npx cap sync ios",
    "cap:open": "npx cap open ios",
    "cap:run": "npx cap run ios"
  },
  "dependencies": {
    "@capacitor/core": "latest",
    "@capacitor/ios": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "latest"
  }
}
